services:
  - type: web
    name: energy-resources-dashboard
    runtime: node
    runtimeVersion: 20
    buildCommand: npm ci --legacy-peer-deps --omit=optional && npm run build
    startCommand: npm start
