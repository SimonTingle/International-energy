services:
  - type: web
    name: energy-resources-dashboard
    runtime: node
    runtimeVersion: 20
    buildCommand: npm ci --prefer-offline --no-audit --ignore-scripts && npm run build
    startCommand: npm start
    envVars:
      - key: npm_config_ignore_scripts
        value: "true"
